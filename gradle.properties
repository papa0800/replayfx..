# Done to match Java's version, 17 or 21 for 1.20.5+
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties
# Confirmed current as of this project's generation (Feb 2026). If a build fails
# with a "could not resolve" error, regenerate this block from
# https://fabricmc.net/develop/template/ (pick Minecraft 1.21.11 + Yarn) and
# paste the fresh numbers in — Fabric ships new builds constantly.
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.1
loader_version=0.18.1

# Mod Properties
mod_version=0.1.0
maven_group=com.replayfx
archives_base_name=replayfx

# Dependencies
fabric_version=0.141.3+1.21.11
